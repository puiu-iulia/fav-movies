class Movie {
    constructor(id, title, imageUrl, rating) {
        this.id = id;
        this.title = title;
        this.imageUrl = imageUrl;
        this.rating = rating;
    }
}

export default Movie;